# CASP17 ligand pipeline — Python dependencies
# (core clustering/scoring env; 문서상 boltz2 env = numpy + gemmi + rdkit + matplotlib)

numpy
gemmi          # 구조(cif/pdb) 파싱
rdkit          # SMILES 처리, 대칭보정 RMSD
matplotlib     # 리간드 PCA·접촉 지문 시각화

# ─────────────────────────────────────────────────────────────
# 아래는 pip 패키지가 아닌 외부 도구/별도 환경 (별도 설치 필요):
#   - gnina        : 도킹 스코어링 (CNN score/affinity + Vina)  — singularity/바이너리
#   - p2rank       : 결합 포켓 예측                              — 외부 바이너리
#   - posebusters  : 포즈 물리 유효성 검사                       — 별도 conda env
#   - AlphaFold3 / Boltz-2 / OpenFold3 / Protenix : co-folding 예측 구조 생성 (입력)
# 서버에서는 scripts/env_setup.sh 가 이들 경로를 함께 설정합니다.
